<div class="post_wrapper">
	<div class="post_content">
		<%= best_in_place_if post.user.id == current_user.id, post, :title, :as => :textarea %><br/>
		<%= best_in_place_if post.user.id == current_user.id, post, :content, :as => :textarea %>
		<small>
			Posted by
			<%= post.user.username %>
			<%= post.created_at.strftime("on %m/%d/%Y") %> 
			<%= post.created_at.strftime("at %I:%M%p") %>
		</small>
		<div>
			<% if post.user.id == current_user.id %>
			<%= link_to "Edit Post", edit_post_path(post)  %>
			<%= link_to "Delete", post, method: :delete, remote: true, class: 'post_delete'%>
			<%= link_to "Full Post", post %>
		<% end %>	
		</div>
	</div>
</div>