let starting = parseInt($("#rep_value<%= @reputatable.id %>").text());
if ("<%= @reputation.rep_given %>" === 'true') {
	$('#rep_value<%= @reputatable.id %>').text(5 + starting);
}else{
	$('#rep_value<%= @reputatable.id %>').text(starting - 5);
};
